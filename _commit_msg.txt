fix data display accuracy and pipeline priority

Issues 1 & 4 — domain_detail cards now show real values + units:
- Non-pct: "10,000 units / 15,000 units target"
- Pct: "65.0% employment rate / 96.0% target"
- Shows "Target met ✓" when pct >= 100%
- Rating line reads "IN PROGRESS — 67% of goal"

Issue 2 — pct_achieved capped at 100% (was 110%); added LOWER_IS_BETTER set

Issue 5 — fetcher fixes:
- employment: regional_employment now = 100 - unemployment_rate (~94-95%)
- employment: KCW-specific StatCan queries with range guard 0.5-20%
- housing: CMHC vacancy range narrowed to 0.1-8.0% (rejects outliers)
- housing: Tavily query targets annual starts with tighter range
- healthcare: ER wait max raised to 8.0hrs (rejects 11hr+ outliers)
- placemaking: GHG Tavily range tightened to 5-40% (was 0-100%)
- all fetchers: priority-level logging (P1/P2/P3)

Issue 6 — source_name stored in DB and shown on cards:
- database.py: source_name column + ALTER TABLE migration for existing DBs
- all fetchers: pass descriptive source_name to insert_result
- db.py: source_name included in all queries
- domain_detail: cards show "✅ Statistics Canada Labour Force Survey" etc
